// Variable are the named container

console.log(10 + 20);

// ways to declare variables
// var    let   const

// used for changeable values

var a = 10;

a = 20;

console.log(a);

// let -> value changeable
let b = 40;

//  const -> constant value
const c = 50;

console.log(b);
console.log(c);

//! Javascript
// single threaded -> run code one task at a time
// interpreted -> run codes line by line
// dynamic typed -> variables can contain any type of data

// ! Difference between var, let, and const

// var -> function scoped, can be re-declared and updated
// let -> block scoped, can be updated but not re-declared
// const -> block scoped, cannot be updated or re-declar
